
export const Tag = ({ label }: {label: string}) => {
    return (
        <div className=" bg-DARK_MIL rounded-[0.313rem] px-3 py-2 text-center line-clamp-2 xl:text-sm">
            {label}
        </div>
    )
}